version: '3.8'

services:
  esphome:
    image: ghcr.io/esphome/esphome:latest
    container_name: cy-esphome
    volumes:
      - /home/andy/buzzgate/compose/esphome/config:/config
    network_mode: host
    privileged: true
    restart: unless-stopped
