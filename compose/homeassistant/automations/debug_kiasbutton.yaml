- id: debug_kiasbutton
  alias: Debug Kiasbutton
  trigger:
    - platform: state
      entity_id: sensor.kiasbutton_action
  action:
    - service: system_log.write
      data:
        message: "Kiasbutton state changed to {{ states('sensor.kiasbutton_action') }}"
        level: info
