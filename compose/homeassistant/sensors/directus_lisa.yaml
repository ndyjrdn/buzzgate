- platform: rest
  name: directus_tasks_lisa
  resource: !secret directus_url_lisa
  method: GET
  headers:
    Authorization: !secret directus_token
    Accept: application/json
  value_template: "{{ value_json.data | length }}"
  json_attributes:
    - data
  scan_interval: 30
