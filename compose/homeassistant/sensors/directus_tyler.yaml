- platform: rest
  name: directus_tasks_tyler
  resource: !secret directus_url_tyler
  method: GET
  headers:
    Authorization: !secret directus_token
  value_template: "{{ value_json.data | length }}"
  json_attributes:
    - data

